import { createStore } from "vuex";

export default createStore({
  state: {
    animefound: true,
    animeSearch: [],
    searchinprogress: true,
    currentList: false,
  },
  mutations: {},
  actions: {},
  modules: {},
});
